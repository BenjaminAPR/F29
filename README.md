# F29 — Micro‑SaaS

El proyecto **Vite + React** de la raíz fue retirado. La aplicación activa es **Next.js (App Router)** dentro de la carpeta `saas/`.

## Arranque

```bash
cd saas
npm install
cp .env.local.example .env.local
npm run dev
```

Documentación detallada: [`saas/README.md`](./saas/README.md).

SQL Supabase (multi‑tenant): [`saas/supabase/schema_multitenant.sql`](./saas/supabase/schema_multitenant.sql).
